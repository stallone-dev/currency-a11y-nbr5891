import { getLogger } from "@logtape";

/**
 * Logger central da biblioteca CalcAUD.
 * Utiliza o framework LogTape para telemetria estruturada e rastreável.
 */
export const Logger = getLogger(["calcaud-nbr-a11y"]);
